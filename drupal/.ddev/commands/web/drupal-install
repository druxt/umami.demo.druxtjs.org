#!/bin/bash

## Description: Install Drupal
## Usage: drupal-install
## Example: "ddev drupal-install"

set -euo pipefail

composer install

# The same fresh install CI and local development use. The committed Tome
# config predates Drupal 10 and still lists ckeditor and rdf, so it cannot be
# imported on Drupal 11; re-exporting it is a separate job.
DRUPAL_DB_URL="mysql://db:db@db/db" .devtools/provision
